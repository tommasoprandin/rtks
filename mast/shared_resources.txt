Shared_Resource (
    Type => Immediate_Ceiling_Resource,
    Name => event_queue,
    Ceiling => 241,
    Preassigned => YES);

Shared_Resource (
    Type => Immediate_Ceiling_Resource,
    Name => task_semaphore,
    Ceiling => 7,
    Preassigned => YES);