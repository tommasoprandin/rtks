\section{Conclusions}

In this technical report, we presented the implementation of a real-time system
in Rust, and we compared it with an equivalent implementation in Ada. The
analysis, conducted with the \textit{MAST} tool, showed that both
implementations are able to achieve similar results in terms of system
utilization. To support the analysis, both systems were also tested on an
\textit{Olimex STM32\-H405} \cite{olimexSTM32H405} using the identified
workloads, and no missed deadlines were observed. Then we tested the validity of
our analysis, in terms of "pessimism" (sensitivity), obtaining consistent
results.

Rust, and in particular the \textit{RTIC} framework, proved to be a
valid alternative for the development of real-time systems.
The \textit{RTIC} framework, being a declarative system model for
single-core applications, enforces a well-defined structure,
requiring a clear understanding and strict control over the management of both
local and shared resources. This strong structure can support developers in
maintaining a well-organized and systematic design of the system.

However, in some situations it may also limit design flexibility in relation to
\textit{Ada Ravenscar Profile}, especially when dealing with shared resources.
In particular, we were unable to separate the responsibility of the
\texttt{on\_call\_producer} activation, as it was not possible to implement it 
except by embedding it directly within the \texttt{deposit} call on the
\texttt{Request\_Buffer}. This limitation stems from the fact that any access to
a shared resource must be explicitly declared in the task’s shared resources
and, consequently, it is not possible for a task to provide an interface through
which other tasks may interact with. The management of shared resources in
\textit{RTIC} always requires acquiring a lock when accessing them: even if
\textit{Stack Resource Policy (SRP)} is lock-free, the lock acquisition is used
to declare critical sections in which tasks can access the acquired shared
resources and invoke on them their methods. In contrast, \textit{Ada Ravenscar
Profile} achieves the same goal through protected objects, which automatically
manage resource access without requiring explicit lock acquisition to delimit
critical sections. Once again, this enforced structure represents the trade-off
between flexibility and the strong safety guarantees provided by \textit{Rust}. 
Moreover, the lack of guard mechanisms for shared resources in \textit{RTIC}
may lead to potential runtime errors if not properly managed by developers. This
requires designing the system carefully, with resources that may need to notify
interested tasks upon status changes. In such cases, the responsibility is
shifted to developers, forcing resources to be aware of the tasks that depend on
them and thereby increasing the coupling between resources and tasks. By contrast,
\textit{Ada Ravenscar Profile} inherently prevents such situations through its
\textit{entry} mechanism, providing more suitable abstractions for managing 
shared resources. 

Unlike the \textit{Ada Ravenscar Profile}, on the subject of deadline miss
detections, the \textit{RTIC} framework reveals a significant limitation by
providing no built-in support for handling such events, thus it leaves the
responsibility for handling timing violations entirely to the developers.

Overall, we consider the \textit{RTIC} framework to be a valid, flexible and
performant option for the development of real-time systems, with a generated
code that, as shown in this report, infers no substantial overhead compared to
an equivalent implementation in \textit{Ada Ravenscar Profile}.

The code relevant for this work is available on GitHub repositories, the URLs
can be found in the references section. The \textit{rtks} repo
\cite{prandinTommasoprandinRtks2025} contains the \textit{Rust} system code,
while the \textit{gee} repo \cite{prandinTommasoprandinGee2025} contains the
\textit{Ada} one. The identification of some \textit{RTIC} system overheads was
performed in the \textit{Profiling-RTIC} repository
\cite{bresolinGianlucaBresolinProfilingRTIC2025}.  
